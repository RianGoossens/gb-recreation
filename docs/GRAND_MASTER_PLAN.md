# Grand Master Plan

The living plan of record. Every task is a checkbox. Work happens one task at a time through the task execution skill. Completing every box here means the project is done.

Rules for this document:
- Organized into playable vertical slices (milestones). Each milestone ends in something you can run and watch.
- Task order is not binding. Milestones are grouped so earlier slices unlock later ones, but within that, pick work by dependency and value, not by list position.
- Mark tasks `[x]` only when done and tested.
- Split a task into subtasks when it turns out bigger than one sitting.
- Add new tasks as reality demands. Keep it honest.

Legend: `[ ]` todo, `[x]` done, `[~]` in progress.

---

## Milestone 0: Workspace and foundations

Goal: the repo builds, tests run, CI is green, the blog deploys.

- [x] Initialize repo, CLAUDE.md, README.md, .gitignore
- [x] Create this plan
- [x] Author the skills (git-github, grand-master-plan, task-execution, testing-validation, dev-blog, self-improvement)
- [x] CI workflow running `cargo test` on push
- [x] GitHub Pages workflow deploying the blog on push
- [x] Scaffold the Rust crate: `cargo` project that builds and has one passing test
- [x] Define the module layout (core logic, rendering, input, assets) as empty documented modules
- [x] First blog post: the workspace and how it is driven
- [x] Push to GitHub, confirm both workflows pass

## Milestone 1: Boot to title screen

Goal: run the app and see the Super Mario Land title screen, rendered by our code.

- [x] Replace the ROM with a hash-verified dump; add a `verify-rom` command that checks SHA-1, MD5, CRC32 and refuses to proceed on mismatch
- [x] Study the title screen logic and tile data (mainly by observing a real emulator; consult the `kaspermeerts/supermarioland` disassembly only where needed); write down the memory map notes in `docs/reference/`
- [x] Asset pipeline: extract the title screen tiles and palette from the verified ROM into our asset format (gitignored output, reproducible command)
  - [x] Tile decoder (2bpp), asset format (save/load + PGM preview), and a ROM-gated `extract-tiles` command that decodes a byte range reproducibly
  - [x] Pin the exact title-screen tile and tilemap source addresses (observe emulator VRAM) and extract them specifically, plus the BGP palette (tools/extract_title.py via PyBoy: signed addressing, map 0x9800, BGP 0xE4)
- [x] Game Boy display model: 160x144 framebuffer, 4-shade palette, tile and tilemap rendering
- [x] Window + rendering frontend that draws the framebuffer to screen (behind the gui feature; building blocks tested, window loop not run in this headless env)
- [x] Headless screenshot command that renders a given state to PNG
- [x] Render the title screen from extracted assets
- [x] Golden-image test: render pipeline guarded by a committed golden of our own demo scene (CI-safe, non-infringing). Title-screen faithfulness is verified locally at 99.82% shade match vs the emulator reference, not committed (copyright).
- [x] Blog post: booting to the title screen

## Milestone 2: World 1-1 renders, Mario walks and falls

Goal: load level 1-1, see it on screen, move Mario left/right with gravity.

- [x] Level data model and 1-1 tilemap extracted from the reference/ROM. Closed: every subtask is done, and the game itself now draws the level with the cartridge's tiles, matching the emulator capture of World 1-1's opening frame in every cell but the two Mario stands in
  - [x] Level data model: solids plus Mario spawn, with a human-editable ASCII loader
  - [x] Extract the scrolling World 1-1 tilemap from the ROM/emulator into level data
    - [x] Pin World 1-1's tile graphics source and the opening screen's background tilemap by observing emulator VRAM (mirrors the title-screen technique; reuses the title screen's existing tile blocks, see docs/reference/level-1-1.md)
    - [x] **Found the cartridge's own collision test.** Super Mario Land tests collision against the background tilemap in video RAM, the same bytes that are on screen; a search of all of 0x8000-0xFFFF for a separate collision copy of a visible column found only the tilemap itself. That makes the game answerable: `tools/probe_solidity.py` writes each of the 256 tile ids into the tilemap in front of Mario and reads the game's own verdict, twice per id (a tall wall four columns ahead, and the ground band replaced). The rule is `id >= 0x60`, with `0xF4` carved out as passable and four ids (`0x68`, `0x69`, `0x6A`, `0x7C`) semi-solid: they hold Mario up but do not block him sideways. All 43 of World 1-1's ids are now settled by direct observation instead of 7 of them. The extracted level is byte-identical to what the previous fitted rule produced, so the fit was right about this level and wrong about why (docs/reference/level-1-1.md)
    - [x] Classify which tile IDs are solid. `tools/classify_solid_tiles.py` settled 7 of the level's 43 ids by watching Mario walk past them (96 solid on 577 frames of support; 44/49/50/51/54/94 non-solid) and left 36 unreachable. Superseded by `tools/probe_solidity.py`, which puts each id in front of Mario instead of waiting for the level to contain it, and settles all 256. The 7 agree
      - [x] Ground (tile 96) confirmed solid and sky/background filler (tile 44) confirmed non-solid, by direct observation of the grounded flag and jump arcs (docs/reference/level-1-1.md)
      - [x] Resolved the "stuck at x=81" mystery: not a blockage, it is the standard mid-screen camera lock (confirmed via frame-by-frame screenshot diffs showing the background scrolling continuously while Mario's screen position stays pinned). No enemy, no wall; the earlier "SCX stays 0" finding was a sampling artifact from the status bar's mid-frame STAT split (docs/reference/level-1-1.md)
      - [x] Pin the step/pyramid structure's solid tiles: **not solid** (against a horizontal approach). Jump-timing sweeps couldn't settle it directly, but building the opening screen into a real level for our own engine (`tools/convert_level_1_1_to_level_format.py`) and walking it did: marking the pyramid solid per the earlier "level-design consistency" presumption made Mario get stuck oscillating at spawn in our engine, contradicting every one of the dozens of real-emulator traces this session showing him walk straight through. Marking it non-solid matches real behavior exactly. Solidity from above (falling onto its top) is untested and separate (docs/reference/level-1-1.md) **Confirmed 2026-07-30** by probing the game directly: the pyramid slope is tile 94 and its base tiles 49/50/51, all below `0x60` and all non-solid to the cartridge's own collision test. Solidity from above is settled too, they support nothing
    - [x] Get a trustworthy world position: `SCX` and a full WRAM/OAM/HRAM scan (`tools/find_scroll_position.py`) both come up empty, so `tools/sml_scroll.py` measures the scroll by cross-correlating consecutive rendered frames instead. Validated at exactly 1 px/frame against the pinned walk speed. This overturned the stitching numbers below: the reactive walker was actually stuck against a pipe at world column 17, covering 137px in 5000 frames, and its reported 626/720/1880 figures came from integrating Mario's WRAM bytes, which go stale rather than blank through the ~150-frame death freeze. A walker that jumps when the measured scroll stalls now reaches world column 78 before its first death, and `stitch_level_1_1.py` stops there and writes a bounded 96-column map (docs/reference/level-1-1.md)
    - [x] Read the level out of the ROM instead of playing through it (Rian, issue 5). All 300 columns of World 1-1 now decode from the cartridge with no emulator involved. The format has two layers: a column record is a run list terminated by `0xFE` (`(row<<4)|count` plus tiles, where count 0 means a full 16 rows and `0xFD <tile>` repeats one tile for the run), and a level is a `0xFF`-terminated list of 16-bit pointers, one per 20-column screen. World 1-1's list is at 0x0A198: 15 screens, 6 of them reused, which is why world columns 0-19 and 40-59 are byte-identical. Verified 88/88 against every column the running game reveals, with the runner-up candidate at 45% (`tools/capture_columns.py`, `tools/decode_level.py --verify`). Found by watching RAM for the game's own banked-ROM read pointer (`tools/find_level_pointer.py`) rather than pattern-matching bytes; that also corrected two earlier wrong offsets and a wrong claim that a tile id could be `0xFE` (docs/reference/level-1-1.md)
    - [x] Stitch the full scrolling width by playing through it. Superseded twice over and now closed. The old "travelled past world column 1880" figures were artifacts of a broken position estimate (the walker was stuck against a pipe at world column 17), and the corrected play-through method still capped at world column 78. The ROM decode above replaces this path entirely. `tools/stitch_level_1_1.py` and the walker stay useful for capturing ground truth to check a decode against, which is what `tools/capture_columns.py` now does (docs/reference/level-1-1.md)
    - [x] Port the level extraction from Python to Rust (Rian, 2026-07-25: Python is fine for prototyping, the finished thing is always Rust). The column-record and screen-list parsing now lives in `src/assets/level.rs` alongside the title-screen extraction, with the solidity rule as named constants (`SOLID_FROM`, `PASSABLE`, `SEMI_SOLID`) sourced to `tools/probe_solidity.py`, and `sml extract-level [out]` writes the level file. The Rust output is byte-identical to what the Python produced, checked by diff. `tools/decode_level.py`, `tools/convert_level_1_1_to_level_format.py` and the superseded `tools/classify_solid_tiles.py` are deleted; `tests/rom_level_decode.rs` carries the regression checks (screen list, the reused-pointer identity, the worked-example column, the nine pits). The PyBoy observation tools stay Python; they are measuring instruments, not product. Extraction runs on demand into a gitignored file, so build-time versus runtime stays open
    - [x] Convert the confirmed grid into `Level`/`Solids` and wire it in, ROM-gated. `tools/convert_level_1_1_to_level_format.py` now reads all 300 columns from the ROM (no emulator) and writes a plain-text level `Level::from_file` loads, still gitignored and regenerated on demand. Solidity is observed where it can be (tile 96 solid; 44/49/50/51/54/94 non-solid) plus two stated inferences: fill propagates downward from a solid cell (without it the raised platform at columns 61-64 is a pit), and a column with no solid cell stands on its lowest non-sky tile (without it the level ends in a pit sixteen columns short of its own exit gate). Tile 99, the pipe, was settled by playing both readings: the cartridge's grounded feet row over that stretch reads `14 14 14 - 10 10 14 14`, our engine with the pipe passable runs flat across it, with it solid it climbs at the same place. `tests/extracted_level_1_1.rs` walks the level to its end trigger and is verified to fail when either inference is removed (docs/reference/level-1-1.md)
- [x] One-way platforms, measured from the cartridge. Four tile ids (`0x68`, `0x69`, `0x6A`, `0x7C`) hold Mario up from above without blocking him sideways or from below. The candidate World 1-2 uses three of them 183 times as horizontal runs with a left cap, repeated middle and right cap over hanging supports, which is the shape of a platform. `Solids` carries a parallel platform layer, written `^` in the level format; landing is decided by where Mario's feet were before the frame's move, and `grounded` ignores a platform while he is rising so a jump through one does not stop at the apex. World 1-1 uses none of them, so its file is unchanged
- [x] Extract World 1-1's coins. Coins are drawn into the background tilemap rather than spawned from an object table, so they fall out of the geometry decode with no object table needed. The coin tile is `0xF4`, found with `tools/find_coin_tile.py` (fly Mario through at a sweep of heights, clear only the solid tiles so the coins survive, and watch which tilemap cell changes when the coin counter moves: `244 -> 44`, thirteen times). This corrects two places that called `0xF4` decoration, including a published post; its "seven-tall bar floating in open sky" is a coin tower. `sml extract-level` writes them as `C`; World 1-1 has 18
- [x] Draw a level with the cartridge's own tile graphics. `sml render-level <level> <column> <out.png>` renders from the ROM with no emulator, scored at 99.60% of playfield pixels against the emulator's own first frame; the only difference is Mario's sprite, which the background renderer does not draw. The earlier note that World 1-1 needed no new ROM offsets was true about the ROM and useless for rendering: gameplay copies the same atlas to different VRAM addresses, and reusing the title screen's layout draws font glyphs. The real layout is one contiguous copy, ROM 0x08032 to VRAM 0x8000, found by locating each chunk of live video RAM in the ROM (`tools/find_gameplay_tile_blocks.py`)
- [x] Scrolling camera that follows Mario
- [x] Mario entity: position, velocity, facing, sprite
- [x] Input mapping (keyboard to Game Boy buttons)
- [x] Walking physics: acceleration, max speed, friction (constants sourced from reference)
  - [x] Verify walking constants against the emulator/disassembly (accel, friction, max walk speed measured from WRAM via tools/find_mario_speed.py)
  - [x] Verify gravity/jump constants against the emulator (a three-regime state machine, not one acceleration; measured via WRAM plus the internal rise/fall phase byte, implemented in `step_motion`; see docs/reference/physics.md)
  - [x] Verify the stomp bounce against the emulator: measured from 61 landed stomps on the first World 1-1 Chibibo (`tools/measure_stomp_bounce.py`), 8px of rise over about 12.4 frames. A bounce decays like a released jump even with the button held, which is a different regime from a jump's held rise, so `Mario` gained a `bouncing` flag. `STOMP_BOUNCE` went 500 -> 360 (docs/reference/physics.md)
- [x] Gravity and ground collision against the tilemap
- [x] Jump physics (initial velocity, variable height)
- [x] Animation states: idle, walk, jump
- [x] Tests: physics constants, collision cases, a scripted-input golden frame (constants pinned; collision floor/wall/ceiling covered; game_walk_right golden)
- [x] Blog post: World 1-1 and the physics of walking

## Milestone 3: Collision, enemies, and death

Goal: full solid-world collision, a Goomba-equivalent enemy that walks and can be stomped, Mario can die.

- [x] Full tile collision (walls, floors, ceilings; one-tile pits). SML tile levels have no slopes, so none are modeled.
- [x] Enemy framework (spawn, update, despawn offscreen)
- [x] The 1-1 first enemy (Goomba equivalent): walk, turn at edges/walls
- [x] Stomp interaction: kill enemy, bounce Mario
- [x] Damage/death: Mario loses on contact, respawn/reset (death animation deferred until Mario has real sprites)
- [x] Tests: enemy movement, stomp vs. side-contact outcomes (walk/wall-turn/ledge-turn/fall; stomp bounce vs. side-contact death)
- [x] Blog post: enemies, stomps, and dying

## Milestone 4: Items, blocks, and scoring

Goal: question blocks, coins, the power-up flow, score and coin counters.

- [x] Interactive blocks (question, brick): bump, spawn contents
- [x] Coins: collect, counter, 100-coin life
- [x] Power-up (mushroom equivalent): spawn, movement, pickup, size/state change
- [x] HUD: score, coins, lives, timer
- [x] Timer countdown and time-out death
- [x] Tests: block bumping, coin counting, power-up state machine (bump gives coin; coin count + 100-coin life + score; power state grow/shrink/die)
- [x] Pin the cartridge's point values: a stomp and a coin are both 100, read off the real status bar (`tools/sml_hud.py` reads score/coins/lives/timer straight from the tilemap, since the digit tiles are the digits; `tools/measure_scores.py` logs every award during a real run). Both already matched what we award. The power-up's 1000 is still unmeasured (docs/reference/faithfulness.md)
- [x] Blog post: blocks, coins, and getting big

## Milestone 5: Level completion and flow

Goal: reach the end of 1-1, complete it, advance. The core loop is playable end to end.

- [x] Level-end trigger and completion sequence
- [x] Level-to-level transition and world map or direct advance (per original)
- [x] Lives and game-over flow
- [x] Title -> play -> die/complete -> title loop closed
- [x] Tests: completion trigger, game-over transition (game: end-completes-and-freezes, lives-out-ends; session: win, advance, game-over, title-return)
- [x] Blog post: closing the loop, a playable slice

## Milestone 6: Breadth (more levels and enemies)

Goal: expand from a vertical slice to coverage of the original game.

- [x] Remaining World 1 levels (the original's real geometry). All three list starts are pinned by playing to them, all three decode from the ROM, and every column of all three matches the running cartridge exactly (300, 280, 300). `sml extract-level 1-1|1-2|1-3` writes each one
  - [x] Verify the World 1-1 decode against the complete level, not a fragment. All 300 columns now match the running cartridge exactly, up from 88. `tools/run_through_levels.py` drives the real game through a whole level by replacing the terrain ahead of Mario with tile 0 (below `0x60`, so not solid) and pinning his Y and phase bytes so the enemies cannot reach him; he reaches 1-1's exit at frame 2299. Capturing needs no camera tracking, since the game writes each column into the tilemap once as it scrolls in and the ring column advances by one each time. Three bugs stood between that and a correct capture, all found by logging the ring index (docs/reference/level-1-1.md)
  - [x] Find the other levels' screen lists. Nothing in the ROM holds World 1-1's list address, so there is no table to follow; `sml scan-levels` finds lists by structure (a `0xFF`-terminated run of in-window pointers where every pointer decodes 20 valid column records). Exactly three exist, matching World 1's three levels: 0x0A190 (contains 1-1's verified list), 0x0A1B7 and 0x0A1DA. The filter deliberately tests for any solid tile rather than for ground, since 1-2 is built on floating platforms and only 40% of its columns have anything solid on the bottom two rows
  - [x] **Pinned where World 1-2 starts: 0x0A1BD**, by playing to it rather than by trusting the six-byte shape. `tools/capture_next_level_opening.py` walks 1-1 with flatten-and-fly, stops poking at the level end (the ending sequence needs Mario to keep walking, and poking through it freezes him at the gate), taps through the bonus game, and matches every screen the game draws against every pointer in the ROM. 1-2 opens on 0x69A6, which only the second list points at. Two false starts: the first match after the level end is 1-1's own thirteenth screen, since a level's tail stays on display, and gating on "matched nothing" fails because the flattened terrain matches nothing either. The bonus game's 2270-frame gap is the reliable marker. `sml extract-level 1-2` gives 280x16, 151 solid cells, 183 platform cells, 26 coins
  - [x] Pinned World 1-3's start: **0x0A1E0**, reached by playing through 1-1 and 1-2. It opens on 0x6E2F, which only the third list points at, and the columns the game draws match the decode
  - [x] Captured World 1-2's columns from the running game: **280 of 280, exact**. Getting past world column 69, where Mario died six runs running, was a matter of the flying height rather than of working out what killed him: swept from a snapshot at 1-2's opening, y 60 loses four lives in 2500 frames and y 32 loses none. Running out of lives sends the game to the title screen's attract demo, which draws real level columns out of order (a block from it matched 1-1 for 93 columns then diverged), so the run stops at game over
  - [x] Wired World 1's three levels in as the default campaign. `session::world_1_levels` loads the extracted files and `session::default_levels` falls back to the placeholder campaign when they are absent, so a checkout without the ROM still runs. The `^` platform marker survives the loader (183 cells in 1-2)
  - [x] Placed the level-end trigger at the cartridge's real exit: a 2x2 door, `0x13 0x21` over `0x24 0x39`, appearing exactly twice per level in the same column (a raised one for the bonus route, one at ground level). The rule it replaces, two columns from the right edge, landed on the door in both 1-1 and 1-2 by coincidence. World 1-3 has no door, since it ends the world rather than leading anywhere, and keeps the far-end placement as a stand-in
  - [x] Finished capturing World 1-3: **300 of 300 columns**, exact. Same fix as before, the flying height: 1-3 is dense (1767 solid cells against 1-1's 922) and the height that crossed 1-2 leaves Mario blocked before the terrain around him clears. y 24 works for all three levels. One run now captures the whole world: 1-1 at frame 2299, 1-2 at 5898, 1-3 at 9320
- [x] A temporary demo campaign to exercise the multi-level flow (placeholder only, until the cartridge's real levels are extracted; shipping our own invented levels is NOT a project goal)
- [x] Remaining enemy types for World 1 (added a hopping Fly alongside the Goomba; more can follow)
- [x] Sound and music model: event model implemented (Game emits SoundEvents; frontend drains them)
- [x] Tone playback: the `gui` frontend plays each SoundEvent as a square-wave beep via `cpal` (src/audio.rs). Frequencies are invented placeholders, not read from the APU; flagged as a stand-in in docs/reference/faithfulness.md.
- [~] Decode the cartridge's object table so the extracted levels carry their real enemies
  - [x] Pinned the record format. A record is three bytes, a list ends on 0xFF, and World 1-1's list is at ROM 0x0A002 (the value 0xD010 holds the moment the level opens, so it is the start by construction). `x` is the position in units of 16 pixels, `y` is the row plus 2, `kind` is what to create. The position mapping came out with no leftover offset: the pointer fires at column count `2x + 5`, the start of a level ties count to scroll at 8 frames per column (`tools/measure_spawn_column.py`), and the object appears 183 pixels right of the camera, so world pixel x is exactly `16x`. Checked against the geometry: all 37 records land on a non-solid cell and all 16 at row 13 have ground beneath. Only 16 records spawn in normal play, exactly those with the top bit of `kind` clear, which was settled by clearing that bit in a scratch copy of the cartridge and watching the record start spawning at the predicted column (`tools/probe_object_type_flag.py`). `sml list-objects 1-1` prints the list (docs/reference/objects.md)
  - [x] Put the cartridge's ground walkers into the extracted levels. Kind 0x00 is the one whose movement is measured (one pixel every three frames, off a ledge rather than turning, straight down at a pixel a frame), so `sml extract-level` writes those records as `G` and leaves the other kinds out rather than guessing at them: World 1-1 gets 10, 1-2 gets 4, 1-3 has none. A level file is short of enemies rather than carrying invented ones
  - [x] Measured what all five of World 1-1's kinds do, on both axes, with the camera frozen (`tools/measure_enemy_walk.py`). 0x00 walks left at 1 px/3 frames and never turns; 0x04 shares that cadence and turned once in 888 frames; 0x0E moves in bursts of up to 4 px per frame in both axes with 54-frame pauses, the shape of something that jumps; 0x0A and 0x0B are the level's last two records, either side of the exit door, and each moves at a flat 1 px/2 frames on one axis only, reversing on a strict 120- and 106-frame cycle over 60 and 53 pixels. A pair of lifts would explain both, which is untested (docs/reference/objects.md)
  - [x] Measured and implemented kind `0x0E`, the jumper. `tools/trace_jumper.py` prints the raw arc with the camera held still, where the walk summary could only call it noise: a 102-frame cycle, 54 frames standing perfectly still and 16 position updates three frames apart, each 2 px sideways with a rise from the fixed table `[4, 4, 2, 2, 1, 1, 1, 0, 0, -1, -1, -1, -2, -2, -4, -4]`, giving a 32 px hop 15 px high. Not constant deceleration, so it is a table in the cartridge and a table in `src/core/enemy.rs`. Turning needed a new instrument: a hopper reverses and rises and falls on its own, so `probe_walker_turn.py` now runs a control with no obstacle and compares distance covered (93 px free, 36 against a wall, 61 into a pit before it fell out of the level). `J` in the level format; World 1-1 gets 3
  - [x] Measured World 1-3's own two kinds, the last unmeasured ones in World 1. `tools/measure_level_kind.py` runs the camera-freeze instrument in any World 1 level, reaching 1-2 and 1-3 through `trace_level_objects.reach_level`, which is the playthrough loop split out of the tracer. Kind `0x02` never moves horizontally and oscillates 16 px down, waits 62 frames, 16 px up, waits 106, on a 200-frame period. Kind `0x0C` never moves horizontally either, stands still, then falls at exactly a pixel a frame with no gaps until it leaves the level. Then settled contact with `tools/probe_object_contact.py`, which writes Mario's position into the game's own bytes and sweeps the whole vertical overlap. Two controls were needed and both caught a bug: a stomp at the top offset made the first version report World 1-1's walker as harmless, and a 180-frame window missed a death that costs a life on frame 212. `0x02` is harmless at every overlap through two full cycles and does not hold Mario up either (with the tilemap carved away he drops straight past it), so it is neither an enemy nor a platform and belongs out of the level. `0x0C`'s sweep matches the walker's exactly, so it is a stompable falling hazard. It stays out until one thing is settled: whether its wait before the fall is a fixed timer or something Mario triggers
  - [x] Counted the whole object roster now that all twelve lists are named: 41 distinct kind bytes, 40 of them in normal play, and the seven measured kinds account for 171 of the 481 normal-play records. Kind 0x36 appears in nine of the twelve levels and has never been watched on the cartridge, which makes it the one worth measuring next (docs/reference/objects.md)
  - [x] **Named the kind bytes.** A name is the one thing about an object that cannot be measured, so this is taken from the `enemies.asm` equate list of the kaspermeerts disassembly and then checked twice. The seven kinds already measured on the cartridge, with no reference to any name, each land on a name matching what they were seen doing (`0x00` Chibibo walks off ledges, `0x04` Nokobon turns at them, `0x0E` Fly hops, `0x0C` Falling Slab drops, `0x0A`/`0x0B` are platforms), and they span `0x00` to `0x0E` so a table off by any amount breaks several at once. The four boss ids each appear exactly once in the cartridge, in the third level of their own world, which corroborates the header's world numbering for worlds 3 and 4 that nothing has ever played to: Hiyoihoi is in the level the table calls 3-3 and Biokinton in 4-3. 40 of the 41 kinds get a name; `0x2F` is left unnamed because the disassembly guesses at it. Two names lose to measurement: `0x0A`/`0x0B` are labelled the wrong way round for their axes, and `0x02` is named for a piranha plant but was harmless at every overlap. `sml list-objects` prints names (docs/reference/objects.md)
  - [x] Confirmed 0x0A and 0x0B are lifts, by dropping Mario onto them (`tools/probe_lift.py`). He lands on each and rests exactly 10 pixels above it, and on the vertical one he rides it back down and up. So World 1-1's last two records are what make its raised exit reachable, and neither is an enemy
  - [x] Implement moving platforms so the lifts can go in
    - [x] Measured a lift's surface (`tools/measure_lift.py`). Mario's feet rest exactly on the top of the row the record decodes to, confirmed two ways: his Y byte is his feet plus 6 (from standing on World 1-1's ground, top edge screen y 128), and on the lift he sits at slot Y minus 10, so his feet land on slot Y minus 16, the slot's Y as a screen coordinate. Horizontally he is held across a 29 px window of his own X. The lift is 16 px wide from its two sprites, and 16 under an 8 px Mario would give a window of 23, so the cartridge's small Mario is nearer 14 px wide than our 8; that is logged as an open discrepancy in Mario rather than fixed by widening the lift. Method note: a save state per offset silently breaks the drop, every offset has to run inside one continuous approach
    - [x] Built the moving platform (`src/core/lift.rs`). One pixel every two frames on one axis, reversing every 120 frames for the vertical one (60 px) and 106 for the horizontal (53 px), 16 px of surface, supporting Mario from above only and carrying him while he stands on it. `V` and `H` in the level format. Two ordering bugs, both caught by tests: reversing before the frame's own move spends that pixel going back and makes a half cycle 58 px instead of 60, and catching Mario only when his feet cross the surface leaves him a pixel behind every frame a descending lift moves, so he never rides it
    - [x] `sml extract-level` writes the lifts from the object list: 2 in World 1-1 (kinds 0x0A and 0x0B, either side of the exit door), 7 in 1-2, none in 1-3. One stated inference: 1-2's records of those kinds are taken to be the same objects as 1-1's, since the kind byte selects the same code. The text level format is a tile grid, so a lift placed at `16x + 8` lands on the column containing it and the half-tile offset is lost
  - [x] Settled what turns kind 0x04 around, by writing a wall and then a pit into the tilemap in front of it (`tools/probe_walker_turn.py`). It turns at both; kind 0x00 turns at the wall and walks off the pit. So the cartridge has two ground walkers with the same speed and different ledge behaviour, and the engine now carries both (`EnemyKind::LedgeTurner`, `T` in the level format). World 1-1 has one of them and 1-2 has four. A trap worth remembering: `Capture` re-arms its flattening for every fresh column, so clearing `pending` once leaves the terrain being erased anyway, and the probe was writing obstacles into ground that was no longer there
  - [x] Pinned all three of World 1's object lists by reading 0xD010 the moment each level opens (`tools/find_object_lists.py`): 1-1 at 0x0A002 (37 records), 1-2 at 0x0A073 (46), 1-3 at 0x0A0FE (48). They sit back to back, with 1-1's terminated by two 0xFF bytes rather than one. The position mapping places every record of 1-1 and 1-2 on a non-solid cell and misses on 17 of 1-3's 48, all of them kinds 1-3 introduces (nine 0x02, seven 0x0C, one 0x36); reading only the low nibble of `y` as the row moves the failures around instead of removing them. The count is pinned in a test so the boundary cannot drift unnoticed
  - [x] Traced World 1-3's spawns and corrected the `y` byte: its low nibble is the row plus 2, its high nibble is a pixel offset added to `x`, so the full position is `16*x + (y>>4)` across and `(y&0x0F)-2` down. 1-1 could not show this (it only uses nibbles 0 and 8, and the whole low seven bits read as a row there), and the bytes alone pointed the wrong way. 1-3 settles it in one frame: two records sharing an `x` byte and a row, differing only in that nibble, are consumed together and spawn 8 pixels apart. `tools/trace_level_objects.py` plays through to any World 1 level and reads the slots. 15 of 1-3's records really do start inside solid tiles, all of kinds 0x02 and 0x0C which only 1-3 uses; the trace confirms the slot position matches the decode, so that is the game's doing rather than ours (docs/reference/objects.md)
  - [x] Found what the skip bit selects for: **expert mode**, the harder replay the cartridge unlocks once the game has been finished. Found by exhaustive search rather than by reasoning: walk until the read pointer sits on a skipped record, snapshot, then for every byte of work RAM and high RAM restore, poke, and walk the same short distance (`tools/find_skip_flag.py`). Two skipped records inside the window means a real flag shows two extra spawns, which throws out the stack pokes that fill all ten slots. 0xFF9A is the only address that qualifies, it reads 0 through every frame of ordinary play, and holding it at 1 across a whole run releases every record at its predicted column (`tools/verify_skip_flag.py`). Expert mode adds to normal play rather than swapping sets, so World 1-1 carries 37 objects instead of 16. The disassembly agrees: `hram.asm` names 0xFF9A `hWinCount`, and `Call_24EF` in bank 0 tests bit 7 only when it is zero. `sml extract-level <level> --expert` writes the expert version (11 ground walkers becomes 26)
- [x] Measured Mario's collision box off the cartridge and adopted it: small Mario is **11 x 12**, not the 8 x 8 the engine had guessed. Wall him into a corridor and the room he had minus the room he used is his width, with the offset between his position bytes and his edges cancelling out of the subtraction (`tools/measure_mario_box.py`). Seven corridors from 24 to 56 px agree on 11, three headrooms agree on 12. Two traps: World 1-1 opens with scenery three columns to Mario's left, so the tool reads the tilemap back to find what actually blocks him rather than trusting its own walls, and a ceiling above the jump's apex returns the apex, so the free jump is measured first and saturated trials are thrown out. Adopting it needed the placeholder campaign to gain a row (a 12 px Mario does not fit under a block one row above the ground) and the golden images regenerated. The lift's 29 px support window is now 3 px wider than 16 + 11 - 1, which moves the open question from Mario to the lift
- [x] Fixed World 1-3's end trigger, which was not merely a stand-in but unreachable. The old fallback (two columns from the right edge at the ground row) put it at column 298 row 13, a one-tile pocket with solid tiles above, below and either side, so the level could never be finished. `level::far_end` now takes the rightmost cell with two free rows (Mario is 12 px tall), solid underneath, and the same true one column left, which rules out the sealed shafts 1-3's tail is full of: column 296, row 11. A flood fill from the spawn to the trigger is now a test across all three levels, and it fails on the old placement. Where the cartridge actually ends 1-3 is still unmeasured
- [x] Implemented the faller (object kind `0x0C`) and gave World 1-3 its enemies, which it had none of. `tools/probe_faller_trigger.py` settled the last question first: catching the slot the frame the game creates the object (it is created off the right of the screen, so waiting for it to scroll in spends part of the count), the fall comes 175 frames later with Mario directly under it, a screen away, and at the left edge. A timer. `D` in the level format; 1-3 gets 6 of its 8 records, the other two starting inside solid tiles the text grid cannot represent
- [x] **Enemies stream in with the camera.** Found while testing the faller: every enemy was created at level start and `despawn_offscreen` deleted the ones past the first screen on frame one, so World 1-1 played with 1 of its 14 enemies and 1-2 and 1-3 with none of theirs. `Game` now keeps unfired spawns in a list and releases each as the camera reaches it, at `camera + 183`, which is where the cartridge puts a new object (slot X `0xBF` as an OAM coordinate). A fired record never returns, matching the single forward read pointer. Three tests had encoded the old behaviour: the geometry walkthrough now clears the enemies (a walker that only jumps gaps dies to the third one), and the spawn-mobility test tracks the furthest Mario reached rather than where he ended, since dying puts him back
- [~] Additional worlds, level by level
  - [x] Found where the other worlds' geometry lives. The decoder had bank 2 written into it, so a screen pointer always resolved to World 1's bank and the scan could only ever find World 1. The bank now comes from wherever a list was found (`offset & !0x3FFF`), and the scan covers every switchable bank. Twelve lists, which is Super Mario Land's twelve levels: six in bank 1 at 0x055BB, three in bank 2 (World 1), three in bank 3 at 0x0D03F. Every one of them decodes to exactly 20 columns per pointer, which a bank mix-up would not
  - [x] Pinned World 2's three levels: **0x055C1, 0x055E8, 0x0560B**, all in bank 1. Found by playing rather than by pattern: `tools/run_through_levels.py` now knows every bank's screens, so it kept going past World 1 and reported 2-1 opening on 0x56CD, 2-2 on 0x5BA3 and 2-3 on 0x6100. Each start is six bytes into the run the scan finds, the same three-pointer prefix every World 1 list carries. The columns settle it: the capture matches all 320 of 2-1's decoded columns and all 280 of 2-2's. The run lost its last life partway through 2-3, so that block runs on into a restart and the test skips it; 2-3 is matched for 261 of its 360 columns and is not yet fully covered. `sml extract-level 2-1` writes it
  - [x] Pinned World 2-1's and 2-2's object lists: **0x05179 and 0x05222**, in bank 1. `0xD010` gives the pointer but not the bank, and reading the bank off the mapped window is wrong (it answers bank 3 for World 1-1, whose list is pinned in bank 2, because the bank switched in at that instant is whatever the game last touched). `tools/find_object_bank.py` asks the spawns instead: play to the level, pair each step of the read pointer with the slot that fills on the same frame, and check the kind byte against all three banks, scoring only frames where exactly one record was consumed and exactly one slot filled. Bank 1 predicts all 37 of 2-1's spawns and all 29 of 2-2's, against 2 and 1 for bank 2; World 1-1 is the control and comes back bank 2, 16 of 16
  - [x] **Found the cartridge's own level table**, which names all twelve lists without playing to them. Every bank holding level data opens with a 0x32-byte header (the tile graphics start right after it, which is why World 1's are at 0x08032 and World 2's at 0x04032): thirteen screen list pointers at 0x00 and twelve object list pointers at 0x1A, both indexed by `world * 3 + level`. The control is the six screen lists and five object lists already measured by playing, and the tables reproduce every one at the index their world and level give, with the level's own first screen six bytes past the entry each time. Bank 1 is the only bank with two distinct triples, so it holds two worlds: World 2 at indices 3 to 5 (measured) and World 4 at 9 to 11. Bank 3's is one triple repeated, and World 3 is what is left. `sml scan-levels` now names all twelve and marks which are measured (docs/reference/level-format.md)
  - [x] Settled World 2-3's object list bank without the run that kept killing the machine. Bank 1's object table holds its pointer 0x529B at index 5, which is 2-3's slot, and the same table reproduces every measured list at its own index. The list is 0x0529B with 39 records, and bank 1's six lists then run back to back from 0x05179 to 0x055B9 with no bytes left over, which is the corroboration
  - [x] Settled what the pointers before a level are: **its bonus rooms**. Every list starts three pointers early (four for 1-1) and what they were for had been open since the first list was found. The last two decode to closed boxes with a solid floor across their whole width, filled with coins, where a level's own screens have gaps; that pair separates all twelve bonus rooms from all six opening screens. A level with one room stores the same pointer twice (World 2-3). `sml bonus-rooms <level>` draws them. The first pointer of the three is still open: World 1's three levels all carry 1-1's opening screen there and World 2's all carry 2-1's, but the lists not yet tied to a level do not follow that
  - [x] Checked World 2's geometry the way World 1's is checked, and fixed what it caught. **2-1 and 2-2 carry the cartridge's own exit door** (the same 2x2 block, twice in one column, raised and at ground level) at columns 318 and 278, so the door rule generalises to a world it was not derived from; 2-3 has none, like 1-3, because it ends the world. The reachability test then failed on 2-3: the far-end fallback had put its trigger in another sealed pocket, since 2-3's tail has pockets two columns wide that pass the neighbour check. `level::far_end` now requires the cell to be somewhere a flood fill from the spawn reaches, which moves 2-3's trigger to column 353 and leaves 1-1, 1-2 and 1-3 where they were. Both playability tests now run over all six pinned levels
  - [x] **Falling out of the level was not fatal.** Found by walking World 2-1: Mario dropped through the water section at column 69 and kept drifting right, 6000 pixels below the level, for the whole 20000-frame run. Nothing in the engine killed him; only the clock and enemy contact did. World 1's levels hid it because the geometry walker jumps every gap in them. `Game::step` now kills him once he is past the bottom of the level, and World 1-1 still walks to its end trigger
  - [x] Recorded how far World 2's geometry lets a plain walker go: 2-1 and 2-2 both stop at column 69 and 2-3 at column 178. 2-1 and 2-2 stop in the same place because both lists point at screen 0x5D32 for world columns 60 to 79 and it has no floor. That was read as water the cartridge expects Mario to swim, which turned out to be wrong: the crossing is by lift (see below). World 2 stays out of the default campaign until something plays it end to end (docs/reference/faithfulness.md)
  - [x] **World 2 was rendering as garbage.** Its geometry is verified column for column against the running game and it still drew nonsense, because a level's tile ids index whatever atlas is in video RAM and that is not one atlas for the whole cartridge. Reading video RAM at 2-1's opening shows most of it shared with World 1 and four spans replaced, three from 0x04032 onward. That is bank 1 plus 0x32, the same offset into its own bank that World 1's 0x08032 is into bank 2, so a world's own tiles sit at the start of the bank holding its levels. `level::tiles_for_level` applies the overlay and 2-1 now draws properly
  - [x] Confirmed the tile overlay is per world, not per level: 2-2's video RAM at level open has a byte-identical block layout to 2-1's, all seven blocks at the same ROM offsets, VRAM addresses and sizes. 2-3 is not measured (the run to reach it kills the machine) but renders as a coherent underwater scene through the same spans
  - [x] `sml scan-levels` now names what it finds: each list's bank, which level it holds, and how many pointers of bonus-room prefix it starts with. Six of the twelve are tied to a level and six read "not yet tied to a level", which is the honest state and makes the remaining work visible from the command itself
  - [x] **Worlds 3 and 4 render**, from the cartridge's own tile-copy tables. The overlay had been measured by searching video RAM at 2-1's opening, which cannot reach a world without playing to it and misses bytes even where it runs (it works in 0x40-byte chunks and drops any that is uniform or not unique in the ROM). The loader in bank 0 at 0x0D8B takes the world number, subtracts two, doubles it, and indexes two pointer tables at 0x0DED and 0x0DF3; each copy runs to a fixed end address, giving 0x8A00 size 0x3D0 and 0x9310 size 0x3F0. Every measured World 2 span sits inside one of those two copies at the same delta, and the fourth measured span turns out to be the shared atlas rather than an overlay. What worlds 3 and 4 then draw checks the header's world numbering independently: 3-1 opens on stone heads under clouds and 4-1 on bamboo under a Chinese key pattern, which are Easton and Chai (docs/reference/level-format.md)
  - [x] Ran the playability checks over all twelve levels. `sml extract-level` writes every one of them, ten of the twelve put Mario on ground he can leave, and every end trigger except World 4-3's is reachable from the spawn by flood fill. The exit door rule holds across all twelve without being touched: the first two levels of every world have the cartridge's 2x2 door and the third has none, because it ends the world. World 4-3 is the exception everywhere: no ground under the spawn, no door, mostly open sky, which is what a vehicle stage looks like. One thing the sweep caught: World 3-2 spawns Mario a step from a two-tile block, so our spawn column (a stand-in, not the cartridge's) needs a jump to leave
  - [x] Wrote worlds 3 and 4 into the faithfulness audit. They are decoded but unplayed: their screen lists, object lists and tile overlays all come from ROM tables rather than from a playthrough, the world numbering is an inference the graphics corroborate, their enemies are held back for the same row question World 2's are, and World 4-3 is a vehicle stage the engine has nothing to fly. Only World 1 is in the default campaign
  - [x] Made all four worlds loadable rather than World 1 alone. `session::world_levels(n)` loads any world's three extracted files, and `sml play` and `sml run` take a cartridge level name ("3-1") where they used to take only a file path, with an error that says which command writes the file. The default campaign is still World 1, since it is the only one that can be finished
  - [x] **The title screen's tilemap comes out of the ROM.** It had been a constant captured from emulator VRAM, with "reimplement the level loader to derive it" logged as open work since Milestone 1. The bank header's thirteenth screen pointer is what the extraction code's own comment called level index 0x0C: the title screen, stored as one screen in the level column format. Decoding it reproduces all 320 cells under the HUD, with the captured map kept as the control. Two things stay captured because the record does not carry them: the two HUD rows the title init routine fills, and one cell at column 14 of screen row 11 that the running game patches
  - [x] **Settled the eight bytes both tile loaders copy to 0xC600**: they are the high bitplane of background tile 0x5D, taken out of the tile data the loader just wrote (the per-world loader from its second source plus 0x2C1, the shared one from bank 2's 0x5603, both landing on video RAM 0x95D1). The routine at 0x02416 writes eight bytes back there every eight frames and bit 3 of the frame counter picks between 0xC600 and a per-world table at 0x3FC4, so the tile has two frames and holds each for eight. World 2's pair pins the table's base and index: its second frame is its first shifted down a row with every row rotated right two pixels, a water surface flowing. The tile is World 2's water line, filling the bottom row of 2-1 and 2-2 up to the dry last screen and the top row of all 360 columns of 2-3 (docs/reference/level-format.md)
  - [x] **The game draws the cartridge's own graphics.** `sml render-level` had rendered a still from the ROM at 99.60% since Milestone 2, but the game loop itself still drew every level as white and dark blocks, because a `Level` is ASCII markers and the tile ids do not survive that. `Level` now carries an optional `Graphics` (a tile id per cell plus the world's 256-tile sheet and its palette), `assets::level::extracted_level` attaches it when the ROM is in the tree, and `Game::new` renders from it when it is there. A hand-written level file has none and keeps the placeholders, so custom levels are unaffected. Coin cells are blanked out of the background, since the game draws coins itself and a painted one would stay behind after Mario took it (docs/reference/level-format.md)
  - [x] **The water animates.** With the cartridge's own tiles on screen, tile 0x5D's two frames have somewhere to go: `Graphics` carries both pictures and `Game::step` swaps the sheet entry every eight frames, the cadence the routine at 0x02416 runs on. Only the high bitplane differs between the two, so both keep the low plane the world loaded. Checked by stepping World 2-1 and reading the eight pixel rows of its bottom row, away from any sprite: they change on frame 8 and come back on frame 16 (tests/extracted_level_1_1.rs)
  - [x] **The status bar is the cartridge's.** The HUD had been our own 3x5 font drawn at invented positions. The cartridge's font is background tiles at the start of the shared atlas (digits 0x01 to 0x09, letters from 0x0A, and zero drawn with 0x18, the same tile as the letter O), and the layout came out of the World 1-1 capture already in the tree: matching every tile of `level_1_1_opening.tiles` against the ROM's own background tiles recovers the two rows as real ids, which read MARIOx02 / WORLD / TIME over score, coin count, world number and clock. `src/hud.rs` builds those two rows and `Game` draws them whenever the level brought cartridge graphics and a level number. Checked pixel for pixel against the capture rendered through the same renderer (tests/extracted_level_1_1.rs)
  - [x] Blog post: "The game draws the cartridge", on the graphics layer, the status bar recovered from a capture already in the tree, the blank-tile bug World 2 caught that a passing test did not, and the animated water line
  - [x] Coins and the exit door draw as the cartridge draws them. A coin is tile 0xF4 out of the world's own sheet rather than the invented mark the engine used, checked by putting the camera on World 1-1's first coin and comparing the eight rows it draws against that tile. The exit door needs nothing drawn at all: it is a 2x2 block of the level's own background, so the engine's pole marker is off for any level that brought cartridge graphics and stays on for hand-written ones
  - [x] **The playfield was drawn behind the status bar.** Comparing our opening frame to the emulator capture cell by cell showed the whole level sitting sixteen pixels too high, with the top two rows hidden under the bar and a white band along the bottom. The level is drawn in the 128 pixels below the bar now, and the camera's vertical window is 128 rather than 144 to match, so a level taller than the cartridge's sixteen rows scrolls instead of losing its bottom rows. That closes the frame: every 8x8 cell of World 1-1's opening matches the capture except the two Mario stands in, 99.83% of pixels, and the test asserts exactly that
  - [x] Drew the bonus rooms. `sml bonus-rooms <level> <prefix>` now writes each room as a PNG through its own world's tile sheet, in the same place on screen the game puts a level, so the sealed coin chambers behind the raised exit door are visible rather than only described in ASCII. A test decodes all twelve levels' rooms and checks each set holds coins and is twenty columns wide
  - [x] Self-improvement pass: recorded in CLAUDE.md that where a reference capture of the whole screen exists, the comparison should cover the whole screen. Two bugs the same day were invisible to tests scoped to the change (the status bar's blank tile, which is empty in World 1 and water in World 2, and the sixteen-pixel playfield offset that had been wrong since the game first drew a level) and both fell out of a full-frame diff against the emulator capture
  - [x] The status bar follows a session from level to level. A session rebuilds its game from a cloned level on every transition and restart, so the world number and the cartridge graphics have to survive the clone; a test plays World 1 through both transitions and compares the two bar rows it renders against the tiles they should be, on all three levels
  - [x] Worlds 3 and 4 get their enemies too. `sml extract-level` takes the object list from the bank header for any of the twelve levels rather than only World 1's three, gated on the same placement check: 3-1 gets 7 walkers and 3 lifts, 3-2 gets 8 walkers, 5 jumpers and a lift, 3-3 gets 2 and 3, 4-1 gets 8 walkers, a faller and 5 lifts, 4-2 gets 16 and 4, and 4-3 has none of the implemented kinds. What is trusted here is that a kind byte selects the same code in World 3 as in World 1, which nothing has watched; the boss ids landing one per world's third level is the corroboration that the lists are paired with the right levels. All twelve counts are pinned in a test
  - [x] **Pinned Mario's graphics in the ROM**, without an emulator. The object atlas has been extracted since the tile copy was found (a level copies ROM 0x08032 into all of VRAM 0x8000 to 0x9800, and the background only reads the top half); what was missing was which tiles are which. The atlas is stored as a picture 16 tiles wide, so a frame is a 2x2 block (`n`, `n+1`, `n+16`, `n+17`) rather than four consecutive ids, and reading it the other way draws a scramble. Small Mario is the first two rows and big Mario the next two, three usable frames each: a still pose and two walking poses. The check that these are Mario is his collision box, measured on the cartridge by walling him into corridors: 11 by 12. The still frame's ink is 10 by 12, sitting on the bottom edge of its block (docs/reference/sprites.md)
  - [x] **Mario is drawn from the cartridge.** `Game` renders his 2x2 sprite block out of the object atlas whenever the level brought cartridge graphics, anchored to his feet (the drawing sits on the bottom edge of its block, so there is no offset to invent) and mirrored when he faces left, which is what the hardware's flip bit does. Colour 0 is transparent, so the background shows through. A hand-written level keeps the placeholder block. Tested by checking every ink pixel of the frame against the shade it should be, and the mirror with the unmirrored comparison as a control that has to fail. Two pieces here are ours and are logged in the audit: which frame plays when, and the object palette
  - [x] The object atlas is partly per world. Comparing the four worlds' sheets tile for tile leaves exactly 61 ids differing, `0xA0` through `0xDC`, which is the band a world's own enemies are drawn from; Mario's tiles are in the shared part, so he looks the same everywhere. `sprite_sheet` takes the world now and a level's graphics carry its own, and `sml sprites <world> <out.png>` draws an atlas so the next enemy work has something to look at
  - [x] Closed two open questions about a level's pointer prefix. The first of the three pointers is the world's opening screen in worlds 1 and 2 and a repeat of one of the level's own coin rooms in worlds 3 and 4, so it is never a third room; `level::prefix_rooms` now reads all three, keeps the ones that decode to rooms and drops repeats, which gives two rooms per level and World 2-3's single one. And each bank's thirteenth screen entry: bank 2's is the title screen, bank 1's repeats its own first level's entry the way its unused slots do, and bank 3's is four pointers to one brick corridor, open across eleven rows, that no level list mentions. What that corridor is for is still open
  - [x] `sml run` takes a world number, so any world can be played as a campaign rather than one level at a time. All twelve levels load with their own graphics, status bar number and enemies now, so the only thing that had been keeping the campaign to World 1 was the argument parser. Tested by driving a real session through World 3's three levels and checking each arrives with its own graphics and world number and renders. World 1 is still the default, since it is the only world that can be finished
  - [x] Self-improvement pass: two things this session taught are in CLAUDE.md now. When the cartridge data is a picture, render it and look at it, which is an observation that needs no emulator, but treat the looking as how you form the reading and find a number from an unrelated route to check it against (the sprite atlas against Mario's measured collision box). And when a property cannot be measured at all, a name being the clearest case, take it from the disassembly and then check the borrowed table's alignment against everything already measured, since alignment rather than content is what goes wrong
  - [x] **The enemies draw with the cartridge's own sprites**, and so do the lifts, which had never been drawn at all: Mario was riding an invisible platform. `tools/measure_object_sprites.py` plays through the worlds and, for every slot holding a kind, reports the OAM entries the game placed near it as offsets from the slot's own position, skipping any object with a neighbour or Mario inside the window. Mario is why: the first run collected big Mario's still frame as if it were kind `0x23`'s own tiles, which incidentally confirmed `mario_frame(Big, 0)` from the running game. Sixteen kinds measured, five of them implemented: Chibibo `0x90`, Nokobon `0x96`/`0x97`, Fly `0xA0` `0xA1` `0xB0` `0xB1`, Falling Slab `0xDD`/`0xDE`, lift `0xEF` three times. The anchor is the same for every kind, the drawing's bottom left corner. The game runs in 8x8 sprite mode, so the Fly's ids are `n`, `n+1`, `n+16`, `n+17`, which confirms the 16-wide sheet from the game rather than from the picture (docs/reference/sprites.md)
  - [x] The object palette stops being a stand-in: `OBP0` reads `0xE4` on the running cartridge and every object sprite measured selects it, which is the value the renderer was already defaulting to
  - [x] **Settled the lift's width, and it explains the number that never fitted.** The engine used 16, the cartridge draws 24, and the support window measured by dropping Mario onto one was 29, which matched neither. Re-sweeping that window a pixel at a time instead of two (`tools/measure_lift.py`) puts it at offsets -2 through +26, exactly 29 rather than roughly. 24 plus a 6 pixel foot is 29, and both edges of the window place that foot at the same spot centred on Mario's position; 16 gives 29 for no foot width at all. So `LIFT_WIDTH` is 24 and `ride_lifts` tests six pixels of Mario rather than his whole eleven, which reproduces the 29 in a test. The sweep also needed a guard: a single-pixel run is long enough that the object leaves the screen and its slot is handed to something else, and what is measured after that is a different object's surface
  - [x] Untangled a name that had come to mean two things. `EnemyKind::Fly` was our own invented hopper, from long before any object list was read, and then the cartridge's kind `0x0E` turned out to be named Fly in the disassembly and was living under `EnemyKind::Hopper`. The cartridge's kind takes its own name now and ours is the `Bouncer`; the level markers `J` and `F` are untouched, since a level file should not break over what we call something. Swept the audit while in there: four enemy rows still said "remains unnamed", and the open discrepancy over the lift's width was still recorded as open
  - [x] **Measured what of an enemy actually hurts Mario: a 5 by 5 box.** Every kind used 8 by 8 in the engine, unmeasured since before any object list was read. `tools/measure_enemy_box.py` holds Mario at every offset from a Chibibo a pixel at a time and watches the life counter, which gives a contact window of 15 across and 16 down; his own box is 11 by 12, measured by an unrelated route, and 15 - 11 + 1 and 16 - 12 + 1 both give 5. The two axes agreeing is the check, since a wrong Mario box would have to be wrong by the same amount on both. The instrument needs both bodies written every frame: a walker covers more than a screen in the 212 frames a death takes to register, so left alone it reaches Mario from any starting offset and every trial reports a hit. Contact is a separate box from the body that walks into walls, which stays 8 by 8 and unmeasured, the same split Mario already has between his box and the 6 pixel foot the lift tests
  - [x] **Mario uses the poses the cartridge picks.** Which frame plays when had been ours. `tools/trace_mario_frames.py` reads the tile of the sprite drawn at Mario's own position every frame alongside the state bytes that could explain the choice: standing is the first block, a walk cycles three blocks (the standing one included) at about four frames each, and every airborne frame is the fourth-across block, through both the rising and falling values of the phase byte, so there is one jump pose and it does not change on the way down. That names a fifth of the eight blocks per row. The full-frame emulator comparison caught a regression on the way in: a `Game` that has never stepped has Mario off the ground, so he drew the airborne pose at a level's opening where the cartridge draws him standing
  - [x] **Measured kind `0x36`, the most-used object in the game** and the one the roster had flagged as worth measuring first: 50 records across nine of the twelve levels, never watched. It never moves (600 frames in World 1-2 with the camera frozen, not a pixel on either axis). It is not an enemy (the whole overlap swept, never a life, against a positive control that lost five out of six). And it goes away the frame Mario's feet reach its surface, at the slot y minus 10 that a lift rests him at, which no other overlap does and which it never does on its own: two controls that already existed. So it is a block that gives way when stood on, which is what its borrowed name says. It stays out of the extracted levels because the one frame it disappears in is exactly the frame that would say whether it holds him first, so the trace cannot separate a crumbling platform from a hand-off to a falling object
  - [x] Checked the hand-typed sprite tables against the ROM, since a transcription error in them would draw the wrong picture and nothing would say so: every tile each table names has to be a drawing in the atlas, no two pieces may sit at the same offset, and the control is that a blank tile exists to be caught (tile `0xFF` was the obvious guess for one and is not blank). A second test pins the band from the other end: every tile the Fly is drawn from differs between worlds and every tile the Chibibo is drawn from does not
  - [x] Read the OAM attribute byte the sprite measurement had already collected. Exactly two of the sixteen kinds, `0x02` and `0x10`, carry bit 7, the hardware's priority bit, so background pixels cover them. That narrows a standing open question: `0x02` is named Pakkun Flower and moves 16 pixels down and back on a cycle, and being drawn behind the background is what lets a plant hide in a pipe rather than slide across the front of it. Why it is harmless at every overlap is still open
  - [x] Self-improvement pass, from three things that actually went wrong this session. CLAUDE.md now says to suspect the *unmeasured* numbers when a measurement disagrees with the model (the lift's 29 was blamed on Mario's measured box for a long time while the lift's own assumed width was the wrong one), to re-measure at finer resolution before theorising about a number (the 29 was rounded from a two-pixel sweep, and only the exact figure could be decomposed), and that a search finding nothing is worth only what its positive control is worth. The dev-blog skill's AI-ism grep now includes digits in its character classes: "24 pixels, not 16" is the same banned construction and the letters-only version walked straight past it in this session's own post
  - [x] **The drop block is settled and implemented**, the largest single block of missing content left: 50 records across nine of the twelve levels. Dropping Mario on it could never answer the question, because he falls three pixels a frame and the object changed on the same frame it would have caught him. `tools/probe_drop_block_support.py` places him at rest height instead, tells the game he is on the ground and then writes nothing more, so what follows is the game's own collision. It holds him for nine frames at the gap a lift holds him at, then descends a pixel a frame and never stops, carrying him the whole way. Its kind byte becomes `0x37` in the same slot rather than emptying, so there is no removal and no hand-off. The negative control is the same position with the slot freed: he falls at three pixels a frame from the first frame. Measured again in 1-3 with the same numbers. Its surface is 8 wide, from a support window of 13, which reproduces the lift's own 6 pixel foot from a different surface width. The sprite survey had skipped the kind because the blocks sit in rows and a neighbour was always inside its window; isolated, it is one tile `0xEE` with the priority bit set. `X` in the level format, 48 records placed. One knock-on: World 3-1's geometry walker now gets to column 98 instead of 53, across a gap one of these bridges
  - [x] **Found the skid pose, and settled the cadence.** Two of the eight blocks in each of Mario's atlas rows are not Mario at all: rendering the atlas shows a dark-haired figure in a robe and, beside a black ball in the small row, a side-view vehicle in the big one, and both turn up in the trace as tiles drawn near him rather than as him. Of the six that are his, block 5 is the skid: `tools/trace_mario_frames.py` catches it twice, at the two moments the input reversed during a run, both times for exactly seven frames, and it is drawn facing the way he is still travelling rather than the way he is pressing, the one pose whose flip does not follow the input. `AnimState::Skid` and `SKID_BLOCK` are in the engine. The cadence does not change with speed: every stretch of the trace holds each block for exactly four frames, walking and with B held, over 28, 33 and 9 changes. Block 3 is the last unaccounted Mario pose, and nothing in the trace draws it (standing, walking, running, jumping, skidding and dying all land elsewhere); swimming is the guess and stays labelled as one. The tracer needed a filter to be readable at all, since a walker Mario is about to meet, and whatever the black ball belongs to, were both being collected as his own poses
  - [x] Measured one edge of an enemy's body, the box that walks into walls, and recorded the other as not measured. `tools/measure_enemy_body.py` writes a wall into the cartridge's own tilemap in front of a Chibibo with the camera frozen: it stops with its slot x four pixels inside the wall's column, at two placements sixteen pixels apart, so the edge the terrain stops is three pixels inside its eight pixel drawing rather than at its left column. The instrument needed a control of its own, and the obvious one was worthless: the ground is continuous, so "the column under it is ground" is true almost anywhere. Taking the floor out from under the object's own computed column is the control that works, and it caught a wrong reading, since an earlier version took the column from the scroll register (which reads 0 throughout, the game scrolling by rewriting the tilemap ring) and the object walked straight through its wall. The other edge needs something walking right into a wall: kind `0x00` never turns and kind `0x04` kept losing its slot mid-run, so `ENEMY_SIZE` stays 8, with a point test at that spot fitting the one reading as well as a box does
  - [x] **There is no swimming to implement.** "Swimming, so World 2's levels can be finished" had been on this list since the geometry walker stopped at column 69 of 2-1 and 2-2, and it was an assumption. Two things say otherwise before any measurement: the decoded tiles put the animated water line in the bottom row of the screen for 300 of 2-1's 320 columns, so the water is below the playfield rather than a body he moves through, and 2-1's own object list puts two horizontal lifts inside the gap. The cartridge settles it (`tools/probe_water.py`): dropped into the gap he neither swims nor drowns, he lands on a lift at the slot y minus 10 a lift rests him at and is carried back and forth by its cycle. A test does the same thing in the engine. World 2-3 is a separate case, with the water line in the *top* row of all 360 columns, so that level is under water throughout, and what it needs is the vehicle rather than a stroke. What is still missing for World 2 is a walker that knows to wait for a lift, not a mechanic
  - [x] **Settled the World 2 spawn rows, and World 2's enemies are extracted.** Two kinds live only in World 2, Honen (0x10) and Yurarin Boo (0x24), and all 38 of their records across the cartridge carry a row byte of 1; no other kind holds one row throughout. The observed Y agrees that the byte is unused for them: 166 is not `8 * row + 16` for any row, so it never came from the record. They place themselves below the screen and swim up. 2-2 matches exactly (2 such records in normal play, 2 disagreements traced); 2-1 has 22 against 23 traced, so one spawn there is unattributed. Extraction is gated on placement instead of on the trace: every record of a kind the engine implements, in all twelve levels, lands on a cell that is not solid, and neither of the two kinds in doubt is among them. `sml extract-level 2-1` now writes 10 walkers and 4 lifts, 2-2 writes 14 and 5, 2-3 has none of the implemented kinds
  - [x] Pinned what actually stops the geometry walker at World 2's water, since "column 69" had been carrying an explanation that turned out to be wrong. The ledge ends at column 59 with a single solid tile at row 9, and the nearest lift's surface is 56 pixels further on and 16 pixels above, on a 53 pixel cycle: a timed running jump onto a moving platform. The walker cannot do that, and both alternatives were tried and are worse (running off the edge reaches 69, waiting at the edge for the lift reaches 58), so 69 is a fact about the bot rather than about the level. A chase that came out of this and is worth recording: the lift's slot y looked like it put every extracted object a row too high, and changing the row offset broke seven tests in one run, all of them right, because a lift's surface is at slot y minus 16 rather than at the slot y
  - [x] **World 1-2's pit can be crossed, and the crossing is two timed jumps.** Three heuristic walkers had failed at it, which was the wrong tool: a jump onto a moving platform is a timing, and the engine is deterministic, so the timing can be searched for. The walker in `world_1_2s_pit_can_be_crossed_by_riding_its_lifts` makes one decision, always the same way (standing with nothing solid two columns ahead, wait `w` frames then jump right), and a greedy search fills that list one entry at a time over the 120 frames of a lift cycle. It finds `[11, 0, 24]`: back up 11 frames at the ledge and jump, hop straight off the first lift, wait 24 frames on the second for it to rise, and land standing at column 117 with no deaths. Three things had to be right before it worked, and each was wrong first: the look-ahead has to be two columns, since by the time the column under his front foot is empty he is already airborne with no decision left to make; the wait has to double as a run-up on terrain and stand still on a lift, since backing up on a 24 pixel platform walks off it; and the search must carry on past a round that improves nothing, since the round that mattered was the one after it. Scoring only counts him while he is standing, because drifting rightwards down a pit covers ground too
  - [x] Carried the searched crossing into the World 1 walkthrough. The session walker now finishes 1-1, hands over, and gets 1-2's first pit behind it, where it used to spend every life. Two things had to change on the way. The single look-ahead had to become two, for two different questions: one column says when to jump, which is at the edge, and two columns say when to think about waiting, because jumping on the further one alone falls short of an ordinary pit and 1-1 stopped finishing. And the candidate range had to cover 240 frames rather than 120, a lift cycle there and back, because 1-2's later lifts only come down to his level on the return leg. An attempt now ends at the first death, which is both faster and the right rule, since a crossing that costs a life is not one
  - [x] **World 1-2's second gap is crossed**, and the thing blocking it was a reading of the level rather than the search. The walker asked `is_solid` for its look-ahead and one-way platforms are not solid, so the gap read as columns 187 to 211 with the nearest footing 96 pixels past the last lift, beyond any jump the engine has. It is columns 187 to 202, ending on a run of platform tiles at column 203, 24 pixels past the lift at 197. `Solids::is_standable` is what a look-ahead should ask, and it has to look from Mario's own row downwards: counting rows above him walks him off column 15 of the same level, where a platform hangs below the ledge he is on. The search is a width-4 beam now rather than a hill climb, since the wait that gets furthest on the second lift can be the one that leaves the third out of reach. Both crossings are tested from a placement on their own ledge, so a failure names the gap it happened in
    - [x] Carried the platform-aware look-ahead into the other walkers, which turned up a second thing wrong with the same question: it read rows from Mario's head down, so a platform level with his chest, 16 pixels above the surface he was standing on, counted as ground he could walk onto. Reading from his feet fixes it. World 2-2's reach went 38 to 52. The World 1 session walker's went 180 to 119, and the 180 was the bug being useful: it walked him off ledges into steps he then blundered up
      - [x] **The World 1 session walker crosses both of 1-2's lift gaps.** It stopped at the 8 pixel step into column 120 because the row to start the look-ahead from was one out: it used Mario's lowest pixel where the question is which surface tops are at or below his feet, so the ledge he was standing on counted as somewhere ahead he could walk to and he stepped off it every time. Reach goes 119 to 220, past the second gap at columns 187 to 202
        - [x] **World 1-2 finishes, third crossing included.** The gap at columns 223 to 236 has one horizontal lift, at column 232, and no plan could ever have crossed it: every lift in the engine set off in the +1 direction because nobody had measured it, and going right this one travels to column 238 and never comes within 72 pixels of the ledge at 222. `tools/measure_lift_phase.py` catches the slot on the frame the game fills it and then freezes the camera, so what follows is the object's own movement: the vertical lift sets off down and the horizontal one sets off **left**, in World 1-1 and again in World 1-2, each running a full half cycle before reversing. Going left it comes back to column 225. The walker needed two fixes of its own, both about the wait: it has to run to completion once started (backing up puts ground ahead again, which stopped the decision firing, so the delay was quantised to an oscillation instead of sweeping the lift's cycle) and it must not back up where there is no ground behind, which is a one-column hole at 219 and the lift itself. The lift he is standing on counts as a decision now, since riding one to the far end of its travel is how this gap is crossed
          - [x] **The walker's jump button was latched shut.** It re-armed the frame its hold ran out, so A was pressed on every single frame and the engine's jump latch (which needs a released frame) never cleared: once stuck it jumped once and then never again, which is how Mario sat in World 1-3's pocket for nine thousand frames pressing jump. Every walker in the tests goes through a `Jump` helper now that puts a release frame in front of each jump. World 2-2's pinned reach went 52 to 63 on the fix alone
          - [x] Pinned what actually stops the World 1 walk. World 1-3 opens with a block at row 10 across columns 7 to 11 and a two-tile wall at columns 13 and 14; walking the floor at row 14 takes him under the block and against the wall, where the wall's top is 16 px above his feet and the block caps his rise 4 px short of it. The level means him to go over the block rather than under it, and the walker has no rule for that. Recorded as a test rather than fixed in passing, since a scripted walker's reach measures the walker
            - [x] Asked the cartridge whether the pocket is a dead end there too, and it is not. `tools/probe_ceiling_cap.py` reaches 1-3, stops the flatten-and-fly the frame the level opens (it prints the tilemap it reads back, which matches the decode column for column) and runs the same walk: holding right at x 88 and tapping A takes Mario's Y byte from 134 to 101, a rise of 33 that puts his feet above the block's own top, against a no-jump control that never leaves 134 and a free-jump control at column 3 that rises 33
              - [x] Narrowed it to the direction he is pressing, and ruled out the other reading. A standing jump at x 88 is capped exactly as ours is (swept a pixel at a time from x 40 to 111: 33 either side of the block, 12 or 16 under it, alternating on an 8 px cadence offset by 6 and running 10 px past its right edge). Pressing each direction with his x pinned gives 12 still, 12 left, 33 right at x 88, and 33 every way at x 48; left and right are the same speed, so a rise crossing the tile between checks is out and the direction is what selects
                - [ ] What the direction selects, which no single tested point fits: the right-pressed point at x 88 has to be at least 8 px ahead of him to clear the block, and that same offset at x 48 would cap him on the block's left column, which it does not
            - [ ] A walker that climbs onto a raised block instead of walking under it, which is what World 1-3's opening needs
  - [x] **Mario could not jump high enough to finish World 1-1, and had not been able to for a long time.** The level's pillar at columns 78 and 79 is 32 px above the ground either side, and the engine's full jump was 26; the walker only ever got past it by releasing A at frame 12, which a wrong `JUMP_CUT` (29, from a quadratic fitted to a noisy segment) turned into a 43 px jump nothing else could match. `tools/measure_jump_height.py` asked the cartridge from one save state, with the standing case reproducing `physics.md` exactly as its control, and got three heights: 24 px standing, 33 with a direction held, 41 with B on top at speed. The traces are identical for twelve frames and then part, so the abrupt stop at the rise cap is what a standing jump does and a moving one keeps rising about a pixel a frame for twelve frames more (`GLIDE_VELOCITY`). `JUMP_CUT` is 76, the value that reproduces both traced peaks when the arc is stepped rather than fitted; the stomp bounce keeps the old number as its own `BOUNCE_CUT`. The geometry walkers' pinned reach moved in eight levels, mostly down, because they no longer have a superhuman jump
    - [ ] The 8 px B adds to a jump is not reproduced. `0xC20C` caps at 6 with or without B, so there is no run speed to hang it on, and what B changes about a jump is unmeasured
- [~] Bosses and special stages
  - [x] Pinned where a boss stands, from the cartridge's data alone. All four records sit exactly eight columns from their level's right edge, and in the three worlds fought on foot the last three columns are a wall with a gap around rows 6 to 9 in the final one, the passage out behind him. The third instance corrected two readings taken from the first two: distance to the end trigger is a fact about `level::far_end` rather than about the room (four columns in 1-3 and 3-3, one in 2-3), and 2-3's boss is over open water rather than on a floor. 4-3 has the eight-column placement and no wall at all, with its end trigger 155 columns behind the boss, which is what a flight looks like and is independent support for it being the vehicle stage
  - [~] Measure what World 1-3's King Totomesu (`0x08`) does on the cartridge
    - [x] **Its movement.** Traced with the camera frozen: x never moves, not a pixel in 700 frames, and y runs 20 px up at one pixel every two frames, 20 back down, then holds still about 81 frames. A 162-frame cycle of leaping straight up on the spot
    - [ ] **What contact does, unmeasured after two attempts, so parked.** The one-offset probe reported the boss harmless and reported an ordinary enemy in the same room harmless too. Sweeping all 41 offsets (`tools/measure_boss_box.py`, which reaches the level once and restores a snapshot per trial) moved the failure rather than fixing it: the control has no window anywhere, so nothing in World 1-3 costs Mario a life at any offset. The suspect is the fly loop that pins his Y byte for thousands of frames to carry him across two levels. The tie-breaker is a control that uses no enemy: walk him into a pit from the same restored state and see whether that costs a life
    - [ ] What finishing it does to the level, which nothing has looked at
  - [ ] The fireball, if the boss fight needs one. Nothing has established whether Mario has one without the superball
  - [ ] 4-3's vehicle stage, which the engine has nothing to fly
- [x] Blog posts per major addition (power-ups and polish post covers the star, superball, pause, one-way camera, sound; more per addition)

## Milestone 7: Moddability

Goal: deliver on the promise that users can make custom levels and mechanics.

- [x] Human-editable level format and loader
- [x] Documentation and an example custom level
- [x] Gate the non-cartridge content so the default build is faithful: `Level::without_non_canonical` drops the invincibility star (SML has none) and the Fly (a generic hopper, not an SML enemy), and `run`/`play` apply it unless given `--allow-non-canonical`. The level format still parses both markers, so a custom level using them loads and simply plays without them (docs/reference/faithfulness.md)
- [x] Hooks or data-driven config for tuning mechanics: `Tuning::from_text` existed and was tested but was never actually reachable from the CLI, and `Session` silently discarded custom tuning on every level transition or restart. Fixed: `run`/`play` both accept an optional tuning file, `Session` carries and reapplies it, and the key reference lives in `docs/reference/level-format.md` (previously nowhere but one blog post's two-line example)
- [x] Blog post: build your own level

---

## Backlog and notes

- Screenshots decision (made by the user, 2026-07-22): game screenshots are fine to use in blog posts as commentary. They are committed under `docs/blog/media/` via Git LFS. The ROM and raw extracted asset files stay gitignored; only curated images go in LFS. So the copyright concern that had parked the image tasks is resolved.
- What still gates the remaining Milestone 1 image tasks is technical, not legal: rendering the real title screen needs the title tile and tilemap data, which means finishing the extraction subtask by observing emulator VRAM (for example with a headless emulator run). That is the next real unblock for "render the title screen", the golden image, and the title-screen blog post.
- The ROM in the tree passes the hash check (verified 2026-07-22).
- Keep physics constants cited to the reference so behavior is defensible.
- Revisit module boundaries at the end of each milestone during self-improvement.
- Jump physics redesign: done. `step_motion` (`src/core/physics.rs`) now models the three measured regimes (near-constant held rise capped by a frame count, real deceleration on early release, real acceleration while falling) instead of one continuous `GRAVITY` acceleration. See `docs/reference/physics.md` for the traces, fits, and a correction made while implementing (the frame-cap-while-held case turned out to be a direct reset, not routed through the release-deceleration constant). Stomp bounce is measured too now, and no physics constant is provisional any more except `MAX_FALL_SPEED`, which is a tunneling guard rather than a modeled behavior.
